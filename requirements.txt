fastapi
uvicorn
numpy
torch
torchaudio
pydantic
omegaconf
pytest
python-multipart
ffmpeg-python
python-dotenv
jinja2
psutil

# Optional ONNX backend (set MODEL_BACKEND=onnx in .env)
# Uncomment if using ONNX backend:
# onnx-asr
# onnxruntime               # CPU only
# onnxruntime-gpu==1.22.0   # CUDA-12 build. Do NOT use latest: newer wheels link
#                           # CUDA 13 (libcudart.so.13) and crash on CUDA-12 images.
